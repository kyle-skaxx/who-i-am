#!/usr/bin/env node
const path = require('path/posix')

require('./src/index')(...process.argv.slice(2)).catch((e) => {
  const progname = path.basename(process.argv[1], '.js')
  console.error(`${progname} failed: ${e.toString()}`)
  process.exitCode = 1
})
