#!/usr/bin/env node
const main = require('./src/index')
const Path = require('path/posix')

main().catch((e) => {
  console.error(`${Path.basename(process.argv[1], '.js')} failed: ${e.message}`)
  process.exitCode = 1
})
